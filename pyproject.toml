[tool.ruff]
line-length = 79
target-version = "py310"
extend-exclude = [
  ".git",
  ".mypy_cache",
  ".ruff_cache",
  "__pycache__",
  "external",
  "mamba_ssm",
  "ot_memory_agent",
  "transformer_killer_core",
  "unified_trans_infinity",
]

[tool.ruff.lint]
select = ["E", "F"]
